import { SearchDishesPipe } from './search-dishes.pipe';

describe('SearchDishesPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchDishesPipe();
    expect(pipe).toBeTruthy();
  });
});
